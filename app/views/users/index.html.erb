

<ul class="users">
  <%= form_tag(users_path , method: "get") do %>
  <%= text_field_tag :search %>
  <%= submit_tag "仲間を探す" %>
  <% end %>
  <% @users.each do |user| %>
    <li>
      <%= link_to user.name, user %>
      <%= user.college_name %>
    </li>
  <% end %>
</ul>
